# Telegram Bot
python-telegram-bot[asyncio]==20.6

# 异步 HTTP 请求
httpx==0.24.1
aiohttp==3.9.5

# 环境变量管理
python-dotenv==1.0.0

# 日志输出（标准库自带，不强制安装，但可选增强库）
# loguru==0.7.2

# JSON 数据处理
# 标准库自带 json 模块，不需要额外安装
